<h1>Courses Home Page</h1>

<h2>Below Find all Courses</h2><br>

<% @courses.each do |course| %>
  <h1> <%= link_to course.name, course_path(course) %> </h1>
  <strong><p>Address:</strong><br>
  <%= course.address %><br>
  <%= course.city %>,
  <%= course.state %></p>
  <strong><p>Description:</strong><br>
  <%= course.description %></p>
  <strong><p>Price: </strong> <br>
  $<%= course.price %></p>
  <strong><p>Difficulty: </strong><br>
  <%= course.difficulty %></p>
  <strong><p>Number of Holes: </strong><br>
  <%= course.holes.count  %></p>
  <strong><p>Par: </strong><br>
  <%=course.total_par %></p>
  <strong><p>Total Distance: </strong><br>
  <%=course.total_distance %> Yards</p>
  <strong><p><%= link_to 'Course Holes', course_holes_path(course)%></strong><br><br>

  Admin:<br>
  <%= link_to 'Add Holes', new_course_hole_path(course) %>
  <%= link_to 'Edit Course', edit_course_path(course) %>
<% end %>