<h1>Courses Home Page</h1>

<h2>Below Find all Courses</h2><br>

<% @courses.each do |course| %>
  <h1> <%= link_to course.name, course_path(course) %> </h1>
  <p>Address: <%= course.address %></p>
  <p>City: <%= course.city %></p>
  <p>State: <%= course.state %></p>
  <p>Description:<%= course.description %></p>
  <p>Price: $<%= course.price %></p>
  <p>Difficulty: <%= course.difficulty %></p>
  <p>Number of Holes: <%= course.holes.count  %></p>
  <p>Par:
  <p><%= link_to 'Click Here To See Course Holes', course_holes_path(course)%><br><br>

  ADMIN:<br>
  <%= link_to 'Add Holes', new_course_hole_path(course) %>
  <%= link_to 'Edit Course', edit_course_path(course) %>
<% end %>